## 项目结构

```bash
├─apis          接口目录
│  └─index.js   接口自动导入
├─assets        内部资源
│  ├─audio
│  ├─fonts
│  ├─images
│  └─videos
├─comps         全局组件目录
│  ├─c1.vue     全局组件c1
│  └─index.js   全局组件自动导入
├─env           环境变量
│  ├─development.env    开发环境
│  └─production.env     发布环境
├─lang          国际化目录
│  ├─zh.vue     中文语言模板
│  ├─en.vue     英文语言模板
│  └─index.js   国际化自动导入
├─layout        布局目录
│  └─default    默认布局
│      ├─index.vue
│      ├─header.vue
│      ├─footer.vue
│      └─menu.vue
├─mixin         全局混合
│  └─index.js   默认混合文件
├─pages         页面目录
│  ├─404        默认404
│  └─index
│      ├─api.js 页面级接口
│      ├─comps  页面组件
│      ├─subPages   子页面目录
│              ├─index.vue    子视图sp1页面
│              ├─routePage.js    子视图sp1路由
│      │  └─sp1     子页面sp1
│      │      └─comps   子页面组件目录
│      │        └─t1.vue   子页面组件t1
│      └─subViews   子视图目录
│          └─sv1    子视图sv1
│              ├─index.vue    子视图sv1页面
│              ├─routeView.js    子视图sv1路由
│              └─comps  子视图组件目录
│                  └─t2.vue   子视图组件t2
├─plugins       插件目录
│    ├─index.js  插件自动导入文件
│    └─basil.js  浏览器存储
├─request       请求目录
│    ├─index.js  api自动导入文件
│    └─api.js    api请求封装
├─router        路由目录
│    └─index.js  路由自动导入文件
├─static        静态资源
│  ├─css
│  ├─js
│  └─images
├─styles        全局样式
│    ├─index.scss       自动导入样式
│    ├─global.scss      全局样式
│    ├─normalize.scss   抹平浏览器差异
│    ├─reset.scss       样式重置
│    └─variable.scss    全局变量
├─tpls          挂载模板
│  └─index.html 默认模板文件
├─utils         通用工具
└─vuex          全局存储
│  └─index.js   默认存储文件
└─App.vue       挂载模板
└─main.js       入口文件
```

## 入口文件

> `src/main.js`

```bash
/**
 * 此文件为应用核心入口文件
 * 一般情况下，不需要动此文件
 * 如果需要添加第三方模块
 * 请到plugins目录下操作
 */
// 核心库
import Vue from 'vue';
// 全局路由
import router from '@src/router/index.js';
// 全局存储
import vuex from '@src/vuex/index.js';
// 国际化
import i18n from '@src/lang/index.js';
// 插件导入
import '@src/plugins/index.js';
// 异步请求
import '@src/request/index.js';
// 接口方法
import '@src/apis/index.js';
// 全局混入
import '@src/mixin/index.js';
// 全局组件
import '@src/comps/index.js';
// 全局样式
import '@src/styles/index.scss';
// 应用模板
import App from '@src/App.vue';
// 实例化
let vm = new Vue({
    router: router,
    store: vuex,
    i18n: i18n,
    render: (h) => h(App)
}).$mount('#app');

```

`yipack`的入口文件，非常的简洁明了，如无必要，请不要在此处引入第三方模块和文件。

## 样式文件

```bash
src/styles
├── global.scss         全局样式
├── index.scss          统一引入
├── normalize.scss      常规样式
├── reset.scss          重置样式
└── variable.scss       变量文件
```

> `yipack` 默认且推荐采用 `scss` 语法，过去，现在，以及将来，也将会支持且仅支持 `scss` 和 `less` 两种预处理语法。（不要问为什么，问就是约定大于配置，为了下一个接手项目的兄弟能开心编码，缩进语法强烈反对者！）

全局样式定义，请在 `global.scss` 文件中定义，不要在 `App.vue` 文件中书写！

默认 `scss` 变量文件为 `variable.scss` ，请将全局变量定义在此文件。

## 路由文件

```bash
src/router
└── index.js
```

> 路由目录有且仅有 `index.js` 这一个文件，也请不要在此目录下增加其他任何东西！使用 `yipack new -p 页面名称` 创建页面，路由是自动生成，自动加载的！
